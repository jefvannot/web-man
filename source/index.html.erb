---
title: Web tricks
---

<div class="welcome text-center padded">

  <% data.homepage_cards.each do |member, infos| %>
  <%= partial(:card, :locals => { :titre => member, :image => infos.image, :link => infos.link }) %>
  <% end %>

</div>
